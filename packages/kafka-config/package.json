{
  "name": "@signalrisk/kafka-config",
  "version": "1.0.0",
  "description": "Shared Kafka configuration, topic constants, and event types for SignalRisk",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "kafkajs": "^2.2.4"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0"
  },
  "files": [
    "dist",
    "src"
  ],
  "license": "UNLICENSED",
  "private": true
}
