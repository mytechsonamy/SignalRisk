apiVersion: v1
kind: Service
metadata:
  name: webhook-service
  namespace: signalrisk
  labels:
    app: webhook-service
spec:
  selector:
    app: webhook-service
  ports:
  - port: 3011
    targetPort: 3011
    protocol: TCP
  type: ClusterIP
