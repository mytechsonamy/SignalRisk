apiVersion: apps/v1
kind: Deployment
metadata:
  name: rule-engine-service
  namespace: signalrisk
  labels:
    app: rule-engine-service
    version: "1.0.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: rule-engine-service
  template:
    metadata:
      labels:
        app: rule-engine-service
        version: "1.0.0"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
      - name: rule-engine-service
        image: signalrisk/rule-engine-service:latest
        ports:
        - containerPort: 3008
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        envFrom:
        - configMapRef:
            name: signalrisk-config
        - secretRef:
            name: signalrisk-secrets
        env:
        - name: SERVICE_NAME
          value: "rule-engine-service"
        - name: PORT
          value: "3008"
        livenessProbe:
          httpGet:
            path: /health
            port: 3008
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 3008
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 3
