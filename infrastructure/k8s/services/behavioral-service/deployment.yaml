apiVersion: apps/v1
kind: Deployment
metadata:
  name: behavioral-service
  namespace: signalrisk
  labels:
    app: behavioral-service
    version: "1.0.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: behavioral-service
  template:
    metadata:
      labels:
        app: behavioral-service
        version: "1.0.0"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
      - name: behavioral-service
        image: signalrisk/behavioral-service:latest
        ports:
        - containerPort: 3005
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        envFrom:
        - configMapRef:
            name: signalrisk-config
        - secretRef:
            name: signalrisk-secrets
        env:
        - name: SERVICE_NAME
          value: "behavioral-service"
        - name: PORT
          value: "3005"
        livenessProbe:
          httpGet:
            path: /health
            port: 3005
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 3005
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 3
