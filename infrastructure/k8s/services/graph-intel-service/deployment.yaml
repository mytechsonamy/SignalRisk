apiVersion: apps/v1
kind: Deployment
metadata:
  name: graph-intel-service
  namespace: signalrisk
  labels:
    app: graph-intel-service
    version: "1.0.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: graph-intel-service
  template:
    metadata:
      labels:
        app: graph-intel-service
        version: "1.0.0"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
      - name: graph-intel-service
        image: signalrisk/graph-intel-service:latest
        ports:
        - containerPort: 3012
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        envFrom:
        - configMapRef:
            name: signalrisk-config
        - secretRef:
            name: signalrisk-secrets
        env:
        - name: SERVICE_NAME
          value: "graph-intel-service"
        - name: PORT
          value: "3012"
        livenessProbe:
          httpGet:
            path: /health
            port: 3012
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 3012
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 3
