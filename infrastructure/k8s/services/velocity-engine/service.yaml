apiVersion: v1
kind: Service
metadata:
  name: velocity-engine
  namespace: signalrisk
  labels:
    app: velocity-engine
spec:
  selector:
    app: velocity-engine
  ports:
  - port: 3004
    targetPort: 3004
    protocol: TCP
  type: ClusterIP
