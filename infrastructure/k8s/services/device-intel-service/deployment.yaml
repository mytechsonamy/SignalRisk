apiVersion: apps/v1
kind: Deployment
metadata:
  name: device-intel-service
  namespace: signalrisk
  labels:
    app: device-intel-service
    version: "1.0.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: device-intel-service
  template:
    metadata:
      labels:
        app: device-intel-service
        version: "1.0.0"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
      - name: device-intel-service
        image: signalrisk/device-intel-service:latest
        ports:
        - containerPort: 3003
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        envFrom:
        - configMapRef:
            name: signalrisk-config
        - secretRef:
            name: signalrisk-secrets
        env:
        - name: SERVICE_NAME
          value: "device-intel-service"
        - name: PORT
          value: "3003"
        livenessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 3003
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 3
