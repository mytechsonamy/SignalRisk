{
  "defaults": {
    "partitions": 48,
    "replication_factor": 3,
    "config": {
      "min.insync.replicas": "2",
      "compression.type": "lz4",
      "message.timestamp.type": "CreateTime",
      "max.message.bytes": "10485760"
    }
  },
  "topics": [
    {
      "name": "signalrisk.events.raw",
      "description": "Main event stream — all incoming transaction/signal events for fraud evaluation",
      "config": {
        "retention.ms": "604800000",
        "cleanup.policy": "delete",
        "segment.bytes": "1073741824"
      }
    },
    {
      "name": "signalrisk.events.dlq",
      "description": "Dead letter queue — events that failed processing after retries",
      "config": {
        "retention.ms": "2592000000",
        "cleanup.policy": "delete",
        "segment.bytes": "536870912"
      }
    },
    {
      "name": "signalrisk.decisions",
      "description": "Decision events — fraud/legitimate verdicts with scores and reasoning",
      "config": {
        "retention.ms": "2592000000",
        "cleanup.policy": "delete",
        "segment.bytes": "1073741824"
      }
    },
    {
      "name": "signalrisk.rules.changes",
      "description": "Rule update notifications — broadcast when detection rules are modified",
      "config": {
        "retention.ms": "-1",
        "cleanup.policy": "compact",
        "min.cleanable.dirty.ratio": "0.5",
        "delete.retention.ms": "86400000"
      }
    },
    {
      "name": "signalrisk.enrichment.telco",
      "description": "Async telco enrichment — SIM-swap, number-porting, and carrier lookups",
      "config": {
        "retention.ms": "86400000",
        "cleanup.policy": "delete",
        "segment.bytes": "536870912"
      }
    },
    {
      "name": "signalrisk.cases",
      "description": "Case creation events — triggers for human review workflows",
      "config": {
        "retention.ms": "2592000000",
        "cleanup.policy": "delete",
        "segment.bytes": "536870912"
      }
    },
    {
      "name": "signalrisk.webhooks",
      "description": "Webhook delivery — outbound event notifications to merchant endpoints",
      "config": {
        "retention.ms": "259200000",
        "cleanup.policy": "delete",
        "segment.bytes": "268435456"
      }
    },
    {
      "name": "signalrisk.consent",
      "description": "Consent change events — POPIA/GDPR consent grants and revocations",
      "config": {
        "retention.ms": "-1",
        "cleanup.policy": "compact",
        "min.cleanable.dirty.ratio": "0.3",
        "delete.retention.ms": "604800000"
      }
    }
  ]
}
