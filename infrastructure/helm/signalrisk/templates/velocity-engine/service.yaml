apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-velocity-engine
spec:
  type: ClusterIP
  selector:
    app: velocity-engine
  ports:
    - port: 3004
      targetPort: 3004
      protocol: TCP
