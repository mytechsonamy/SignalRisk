apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-case-service
spec:
  type: ClusterIP
  selector:
    app: case-service
  ports:
    - port: 3010
      targetPort: 3010
      protocol: TCP
