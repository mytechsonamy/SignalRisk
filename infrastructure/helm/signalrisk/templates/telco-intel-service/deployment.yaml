apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-telco-intel-service
  labels:
    app: telco-intel-service
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
spec:
  replicas: {{ .Values.telcoIntelService.replicaCount | default .Values.defaults.replicaCount }}
  selector:
    matchLabels:
      app: telco-intel-service
  template:
    metadata:
      labels:
        app: telco-intel-service
    spec:
      containers:
        - name: telco-intel-service
          image: "{{ .Values.global.imageRegistry }}/telco-intel-service:{{ .Values.global.imageTag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: 3007
          readinessProbe:
            httpGet:
              path: /health
              port: 3007
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3007
            initialDelaySeconds: 30
            periodSeconds: 30
          resources:
            {{- toYaml .Values.defaults.resources | nindent 12 }}
          envFrom:
            - configMapRef:
                name: {{ .Release.Name }}-telco-intel-service-config
            - secretRef:
                name: {{ .Release.Name }}-telco-intel-service-secrets
                optional: true
