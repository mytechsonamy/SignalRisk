apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-webhook-service
spec:
  type: ClusterIP
  selector:
    app: webhook-service
  ports:
    - port: 3011
      targetPort: 3011
      protocol: TCP
