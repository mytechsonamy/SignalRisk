apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-rule-engine-service
spec:
  type: ClusterIP
  selector:
    app: rule-engine-service
  ports:
    - port: 3008
      targetPort: 3008
      protocol: TCP
