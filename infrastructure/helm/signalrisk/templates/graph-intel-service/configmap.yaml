apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-graph-intel-service-config
data:
  PORT: "3012"
  NODE_ENV: "production"
  {{- range $key, $val := .Values.graphIntelService.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
