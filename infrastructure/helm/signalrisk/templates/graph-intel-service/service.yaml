apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-graph-intel-service
spec:
  type: ClusterIP
  selector:
    app: graph-intel-service
  ports:
    - port: 3012
      targetPort: 3012
      protocol: TCP
